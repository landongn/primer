/*
module('ExampleMixin');

test('ExampleMixin is an Ember Mixin', function () {
	ok(Ember.Mixin.detect(App.ExampleMixin), "App.ExampleMixin is an Ember.Mixin subclass");
});

test('ExampleMixin should call methods properly', function () {
	var ObjectWithMixin = Ember.Object.extend(App.ExampleMixin, {});

	var item = ObjectWithMixin.create();

	item.set('example-property', true);

	equal(item.get('other-property'), 'My Computed Value', "Setting example-property to true should change the other-property");
});
*/
